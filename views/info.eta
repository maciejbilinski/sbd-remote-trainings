<% layout('./layout', {styles: ['form'], scripts: ['back']}) %>
<section class="content" data-bs-theme="light">
    <h1 class="text-center">
        <%= it.info %> <%= it.what %>!
    </h1>
    <div class="text-center">
        <% if(it.error){ %>
            <p class="error"><%= it.error %></p>
        <% } %>
        <button class="btn btn-primary" id="cockpit-btn">Powrót</button>
    </div>
</section>